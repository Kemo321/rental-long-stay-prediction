version: '3.8'

services:
  nocarz-api:
    build: .
    container_name: nocarz_predictor_service
    ports:
      - "8080:8080"
    volumes:
      - ./logs:/app/logs
    environment:
      - LOG_PATH=/app/logs/ab_test_logs.jsonl
    restart: always